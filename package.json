{
  "name": "node_assignment",
  "version": "1.0.0",
  "description": "REST API server with MongoDB",
  "main": "dist/server.js",
  "scripts": {
    "start": "ts-node src/server.ts",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.0.2",
    "ts-node": "^10.9.1",
    "@types/node": "^18.15.3"
  },
  "dependencies": {
    "mongodb": "^5.1.0"
  }
}
